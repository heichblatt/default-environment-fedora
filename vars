if [[ $(whoami) == root ]] ; then
	SUDO=""
else
	SUDO=$(which sudo)
fi
INSTALL=${INSTALL:-"$SUDO yum -y install"}
GROUPINSTALL=${GROUPINSTALL:-"$SUDO yum -y groupinstall"}
LOCALINSTALL=${LOCALINSTALL:-"$SUDO yum -y localinstall"}
GEMINSTALL=${INSTALL:-"$SUDO gem install"}